#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("input.text", "r", stdin);
    freopen("output.txt", "w", stdout);

    int n;
    cin>>n;
    cout<<"You entered: "<<n;

    return 0;
}